# Repository Collaboration Rules

이 워크스페이스는 Codex 단독으로 이슈 단위 작업을 수행한다. 이슈는 기능 단위로 만들고, 기능 이슈 하나는 기본적으로 브랜치 하나에 대응한다.

## Module Boundaries

- `drrk_BE/common`은 공용 라이브러리 모듈이며 Gradle 경로는 `:common`이다. 두 실행 모듈이 함께 사용하는 도메인 모델, DTO, 저장소 계약, 공용 설정, 공용 유틸리티만 둔다.
- `drrk_BE/app-main`은 메인 실행 모듈이며 Gradle 경로는 `:main`이다. 핵심 REST API, 인증과 보안, 서비스, 영속성, Redis 연동, 메시지 소비 등 메인 서버 전용 구현을 둔다.
- `drrk_BE/app-collector`는 수집 실행 모듈이며 Gradle 경로는 `:collector`이다. 외부 데이터 수집, 외부 시스템 클라이언트, 수집 API, 메시지 발행 등 수집 서버 전용 구현을 둔다.
- `app-main`과 `app-collector`는 서로 직접 의존하지 않는다. 함께 사용하는 코드만 `common`을 통해 공유한다.
- 한 실행 모듈에서만 사용하는 코드는 `common`으로 올리지 않고 해당 실행 모듈 안에 둔다.
- `common`은 실행 모듈의 코드나 설정에 의존하지 않는다.

## Codex Workflow

- 설계, 구현, 테스트, 검증은 Codex 안에서 수행한다.
- 특정 Codex 모델은 이 문서에서 고정하지 않고 실행 환경에서 선택한다.
- 작업 범위가 작고 명확하면 Codex가 직접 끝까지 수행한다.
- 서로 독립적인 작업을 병렬화할 가치가 있을 때만 Codex 네이티브 서브에이전트를 사용한다.
- 외부 AI 도구를 필수 설계 검토 단계로 두지 않는다.

## Branching

- 베이스 브랜치는 항상 `develop`이다.
- 작업 흐름은 `develop -> 이슈 브랜치 -> 필요한 경우 서브브랜치` 순서로 가져간다.
- 이슈 브랜치는 기능 이슈 단위로 생성한다.
- 서브브랜치는 큰 이슈를 안전하게 나누거나 실험이 필요한 경우에만 만든다.

## Planning Files

- 구현 전에 루트 `spec.md`에 현재 이슈의 목표, 범위, 설계, 검증 방법을 작성한다.
- 사용자가 설계를 요청하면 결과를 루트 `spec.md`에 기록한다.
- `spec.md`는 핵심 결정, 주요 흐름, 검증 방법 위주로 짧고 읽기 쉽게 작성한다. 사용자가 요구하지 않은 세부 구현 설명으로 문서를 과도하게 늘리지 않는다.
- `spec.md`를 새로 만들거나 수정하기 전에 `docs/spec-template.md`를 읽고 해당 문서의 필수 구조, 작성 규칙, 완료 점검을 따른다.
- `docs/spec-template.md`와 이 문서의 규칙이 충돌하면 `AGENTS.md`를 우선한다.
- 루트 `memory.md`에는 진행 상황, 주요 결정, 완료한 검증, 다음 작업을 기록한다.
- 설계 문서는 루트 `spec.md` 하나만 사용한다.
- 구현 중 별도의 계획 문서나 하위 `spec.md`를 추가로 만들지 않는다.
- 중요한 상태가 바뀌면 루트 `memory.md`만 간결하게 갱신한다.
- `spec.md`와 `memory.md`는 로컬 Codex 작업 문서로 취급하며 커밋하지 않는다.

권장 기본 구조:

```text
Tomorrow_Hackathon/
├─ AGENTS.md
├─ docs/
│  └─ spec-template.md
├─ spec.md
├─ memory.md
└─ drrk_BE/
   ├─ common/
   ├─ app-main/
   └─ app-collector/
```

추가 설계 문서가 필요해 보여도 먼저 루트 `spec.md`에 반영하고, 별도 파일을 만들지 않는다.

## Implementation Method

- 구현은 Codex에서 제공되는 Superpowers의 TDD 흐름을 사용한다.
- 기능을 작게 나누고 실패하는 테스트를 먼저 작성한 뒤 최소 구현과 리팩터링을 진행한다.
- 세부 작업이 끝날 때마다 관련 테스트를 실행하고 결과를 확인한다.
- 작업 상태, 테스트 결과, 다음 작업은 `memory.md`에 간결하게 갱신한다.
- 새 의존성이나 불필요한 추상화보다 기존 코드와 유틸리티를 우선한다.

## Verification

- 변경한 모듈의 타깃 테스트를 먼저 실행한다.
- 이후 영향 범위에 맞게 전체 테스트, 정적 분석, 빌드 검증을 수행한다.
- 검증에 실패하면 원인을 해결하고 같은 명령을 다시 실행한다.
- 실행할 수 없는 검증이 있으면 그 이유와 대신 수행한 검증을 `memory.md`와 PR 본문에 기록한다.
- 검증 결과를 직접 확인하기 전에는 완료로 보고하지 않는다.

## Commits

- 세부 작업 하나마다 커밋 하나를 만든다.
- 커밋 메시지는 한글로 작성한다.
- 커밋 메시지는 Conventional Commits prefix를 반드시 붙인다.
- 허용 prefix는 `feat`, `fix`, `refactor`, `test`, `docs`, `chore`, `build`, `ci`, `perf`, `style`, `revert`를 기본으로 한다.
- 커밋 메시지는 `{prefix}: {한글 설명}` 형식으로 작성한다.
- 예: `feat: 회원가입 요청 검증 추가`, `test: 수집 메시지 발행 테스트 보강`, `refactor: 공용 응답 DTO 정리`
- 반드시 작업자의 허락을 통해 푸시를 한다.
- 작업자가 커밋 기준을 보고 판단할 수 있게 구현 후에 마지막으로 커밋 메시지를 나열해서 보여준다.

## Pull Requests

- PR은 저장소의 PR 템플릿에 맞춰 작성한다.
- 구현한 기능과 세부사항을 명확하게 적는다.
- 연결된 이슈 번호, 주요 변경 파일, 테스트 결과, 남은 위험 또는 후속 작업을 포함한다.
- PR을 올리기 전에 저장소 CI에서 수행하는 검증을 가능한 범위까지 로컬에서 먼저 실행한다.
- PR마다 CodeRabbit 검토를 실행한다.
- CodeRabbit 지적의 반영 여부는 작업자가 코드와 요구사항을 확인한 뒤 판단한다.
